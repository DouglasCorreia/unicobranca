export const windowWidth = window.screen.width;
export const isMobile = windowWidth <= 1024;
export const body = document.querySelector('body');
export const header = document.querySelector('header.header');
export const footer = document.querySelector('footer.footer');
